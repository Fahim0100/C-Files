#include <stdio.h>
#include <math.h>
//to use math fuction such as log, sin, sqrt

int main(){

    double A = sqrt(36);
    double B = pow(4,4);
    int c = round(3.4); //it rounds the number to the closest number
    int D = ceil(3.1); // ceil round ups the fraction number
    int E = floor(4.9); // floor round downs the fraction number
    double F = fabs(650); // how far a number is from zero
    double G = log(9);
    double H = sin(90);
    double I = cos(0);
    double j = tan(45);

    printf("%lf", A);

    return 0; 
}